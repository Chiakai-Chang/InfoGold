# InfoGold - 原始逐字稿存放資料夾 (test/)

此資料夾專門用於存放您的原始語音轉文字逐字稿（如 `.txt` 檔案）。

### 💡 使用說明
1. **免指定路徑執行**：直接將您要處理的原始文本（例如 `會議逐字稿範例.txt`）放入本資料夾內。當您呼叫 InfoGold 時，AI 將會自動讀取此資料夾下的文字檔進行處理。
2. **隱私與安全安全（Git Ignore）**：本資料夾內除了本 `.gitkeep` 說明檔外，其餘所有文字檔（.txt、.log、.txt 副本等）皆已在 `.gitignore` 設定排除，**絕對不會被 commit 或上傳到遠端 GitHub**，保障您的公務或商業機密安全。
